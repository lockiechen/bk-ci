import { defineComponent } from 'vue'

export default defineComponent({
  name: 'StartParams',
  setup() {
    return () => (
      <div>
        <h3>启动参数</h3>
        {/* TODO: 实现启动参数的具体内容 */}
      </div>
    )
  },
})
